import { MockerRouter } from './router';
export interface IMockerServer {
    readonly isLegacy: boolean;
    readonly router: MockerRouter;
}
export declare class MockerServer implements IMockerServer {
    readonly isLegacy: boolean;
    readonly router: MockerRouter;
    constructor(baseURL?: string);
}
