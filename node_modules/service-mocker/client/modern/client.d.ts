/// <reference types="service_worker_api" />
import { IMockerClient } from '../client';
export declare class ModernClient implements IMockerClient {
    readonly isLegacy: boolean;
    readonly ready: Promise<ServiceWorkerRegistration>;
    controller: ServiceWorker;
    constructor(scriptURL: string);
    update(): Promise<ServiceWorkerRegistration>;
    getRegistration(): Promise<ServiceWorkerRegistration>;
    unregister(): Promise<boolean>;
    private _init(scriptURL);
    private _autoSyncClient();
}
