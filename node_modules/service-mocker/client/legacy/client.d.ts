import { IMockerClient } from '../client';
export declare class LegacyClient implements IMockerClient {
    readonly isLegacy: boolean;
    readonly ready: Promise<null>;
    controller: null;
    private _registration;
    constructor(scriptURL: string);
    update(): Promise<null>;
    getRegistration(): Promise<null>;
    unregister(): Promise<boolean>;
}
